name: Sync Upstream

on:
  # Upstream syncing is intentionally manual now; this fork has diverged substantially.
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  pull-requests: write

jobs:
  sync:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          ref: main

      - name: Fetch upstream
        run: |
          git remote add upstream https://github.com/jarnedemeulemeester/findroid.git
          git fetch upstream main

      - name: Check for new upstream commits
        id: check
        run: |
          if git merge-base --is-ancestor upstream/main HEAD; then
            echo "No new upstream commits."
            echo "has_new=false" >> "$GITHUB_OUTPUT"
          else
            echo "has_new=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Update sync-upstream branch
        if: steps.check.outputs.has_new == 'true'
        run: |
          git branch -f sync-upstream upstream/main
          git push origin sync-upstream --force

      - name: Generate commit summary
        if: steps.check.outputs.has_new == 'true'
        id: summary
        run: |
          commit_count=$(git rev-list --count main..upstream/main)
          {
            echo "body<<GB_SYNC_EOF"
            echo "Automated sync of new commits from the upstream repository (https://github.com/jarnedemeulemeester/findroid). Review and merge to bring them into main."
            echo ""
            echo "## New commits ($commit_count)"
            echo ""
            git log --no-merges --pretty='format:- %h %s (%an)' main..upstream/main | head -100
            if [ "$commit_count" -gt 100 ]; then
              echo ""
              echo "_...and $((commit_count - 100)) more, truncated._"
            fi
            echo ""
            echo "GB_SYNC_EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Ensure a single open sync PR
        if: steps.check.outputs.has_new == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          existing=$(gh pr list --head sync-upstream --state open --json number --jq '.[0].number // empty')
          if [ -z "$existing" ]; then
            gh pr create \
              --base main \
              --head sync-upstream \
              --title "Sync with upstream (jarnedemeulemeester/findroid)" \
              --body "${{ steps.summary.outputs.body }}"
          else
            gh pr edit "$existing" --body "${{ steps.summary.outputs.body }}"
            echo "PR #$existing already open; branch push above already updated it, body refreshed with latest commit summary."
          fi
